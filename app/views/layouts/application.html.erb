<!DOCTYPE html>
<html>
  <head>
    <title>PlaceosAuth</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <style>
      body {
        margin: 0;
        border:0;
        font-family: Roboto, sans-serif;
        color: white;
      }
      hr {
        height: 0;
        width:100%;
        color: #999;
      }
      .container {
        display: flex;
        align-items: center;
        justify-items: center;
        background-color: #051c2c;
        border:0;
        height: 100vh;
      }
      .msg-container {
        display: flex;
        flex-direction: column;
        margin: auto;
        max-width: 768px;
        align-items: center;
      }
      #message {
        font-size: 2.5rem;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
